{
  "name": "col-tablebase-app",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop app for running and inspecting Col tablebases.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js"
  },
  "devDependencies": {
    "electron": "^33.4.11"
  }
}
